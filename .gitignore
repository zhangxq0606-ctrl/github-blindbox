node_modules/
.env
*.log
# 本地测试产物
digest.txt
digest-stderr.txt
# 本地架构说明，仅供 AI 修改操作时阅读，不对外发布
ARCHITECTURE.md
# 历史去重持久化文件（推荐过的项目 fullName 列表）
.trending-history.json
.trending-history-state.json
# 服务器端数据缓存目录（fetch-trending.sh 写入，不应入库）
cache/
# Trae IDE 本地规则文件（含服务器 IP/SSH 路径等运维信息，禁止入库）
.trae/